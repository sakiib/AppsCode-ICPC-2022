apiVersion: batch/v1
kind: Job
metadata:
  name: form-teams
  namespace: demo
spec:
  template:
    spec:
      containers:
      - name: contest
        image: sakibalamin/contest:0.0.1
      restartPolicy: Never
